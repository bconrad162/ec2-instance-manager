[package]
name = "ec2_manager"
version = "0.1.0"
edition = "2021"

[features]
default = []
gui = ["dep:eframe", "dep:portable-pty", "dep:vt100"]

[dependencies]
eframe = { version = "0.33.3", optional = true }
portable-pty = { version = "0.9", optional = true }
vt100 = { version = "0.16.2", optional = true }
